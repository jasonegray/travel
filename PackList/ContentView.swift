import SwiftUI

struct ContentView: View {
    var body: some View {
        NavigationStack {
            Text("PackList")
                .navigationTitle("PackList")
        }
    }
}

#Preview {
    ContentView()
}
